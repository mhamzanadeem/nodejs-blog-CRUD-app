<nav class="navbar navbar-expand-lg bg-white py-3">
  <div class="container">
    <a class="brand-text" href="/">Medium<span class="brand-dot">.</span></a>
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav align-items-center gap-3">
        <% if (typeof user !== 'undefined' && user) { %>
          <li class="nav-item">
            <a class="btn btn-medium" href="/blog/add-new">
              <i class="fa-regular fa-pen-to-square me-1"></i>Write
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/user/logout" style="font-family:Arial,sans-serif;color:var(--medium-gray);font-size:0.9rem;">
              <i class="fa-solid fa-arrow-right-from-bracket me-1"></i>Sign Out
            </a>
          </li>
          <li class="nav-item">
            <form action="/user/delete-account" method="POST" onsubmit="return confirm('Delete your account permanently? All your stories and comments will be removed.')" class="m-0 p-0">
              <button type="submit" class="btn nav-link text-danger border-0 bg-transparent" style="font-family:Arial,sans-serif;font-size:0.9rem;">
                <i class="fa-regular fa-trash-can me-1"></i>Delete Account
              </button>
            </form>
          </li>
          <li class="nav-item">
            <img src="<%= user.profileImageURL %>" alt="<%= user.fullName %>" class="avatar-sm" />
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/user/signin" style="font-family:Arial,sans-serif;color:var(--medium-dark);font-size:0.9rem;">Sign In</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-medium" href="/user/signup">Get Started</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
