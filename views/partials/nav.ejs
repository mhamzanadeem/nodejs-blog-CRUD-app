<nav class="navbar navbar-expand-lg bg-white py-3">
  <div class="container">
    <a class="brand-text" href="/">Medium<span class="brand-dot">.</span></a>
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav align-items-center gap-3">
        <% if (typeof user !== 'undefined' && user) { %>
          <li class="nav-item">
            <a class="btn btn-medium" href="/blog/add-new">
              <i class="fa-regular fa-pen-to-square me-1"></i>Write
            </a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center gap-2 p-0" href="#" role="button" data-bs-toggle="dropdown">
              <img src="<%= user.profileImageURL %>" alt="<%= user.fullName %>" class="avatar-sm" />
              <span class="d-none d-lg-inline" style="font-family:Arial,sans-serif;font-size:0.9rem;"><%= user.fullName %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow-sm" style="border-radius:8px;border:1px solid var(--medium-border);">
              <li><a class="dropdown-item" href="/user/logout" style="font-family:Arial,sans-serif;font-size:0.9rem;"><i class="fa-solid fa-arrow-right-from-bracket me-2"></i>Sign Out</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/user/signin" style="font-family:Arial,sans-serif;color:var(--medium-dark);font-size:0.9rem;">Sign In</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-medium" href="/user/signup">Get Started</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
